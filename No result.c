#include <stdio.h>

void main()

{
    int a, b;

        printf("Enter two numbers: ");
        scanf("%d%d", &a, &b); //scans for both integers before printing
        printf("The result of the numbers is: %d", a+b); //once both are given it will print the resultant

}