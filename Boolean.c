#include <stdbool.h>
#include <stdio.h>

int main(void)
{
    bool a;
    
    a = 4; printf("%d", a); // 1
    a = 0; printf("%d", a); // 0
    a = 1; printf("%d", a); // 1
   

    return 0;  // have to include now that, int main(void)
}